<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph">

    <activity
        android:id="@+id/mainStart"
        android:name="com.example.baza.MainStart"
        android:label="activity_main_start"
        tools:layout="@layout/activity_main_start" >
        <action
            android:id="@+id/action_mainStart_to_mainActivity"
            app:destination="@id/mainActivity" />
    </activity>
    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.baza.MainActivity"
        android:label="MainActivity" >
        <action
            android:id="@+id/action_mainActivity_to_mainStart"
            app:destination="@id/mainStart" />
    </activity>
</navigation>